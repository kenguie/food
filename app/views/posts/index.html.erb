<%= render "shared/nav" %>

<div class="container-foo"> <!-- Wrap your grid in a container to control its width. Giving your container a max-width of 960px and centering it using margin: 0 auto; is a good place to start. -->
  <div class="section group"> <!-- This is a row -->
    <div class="col s1">...</div> <!-- These are columns -->
    <div class="col s2">...</div>
  </div>
</div>

<h1 id="heading">History of Questions asked and Comments made</h1>
<h3 id="expert">Any name in <span class="expert"> green </span> will be a name of an expert in our field.</h3>

  <ul>
    <% @posts.each do |post| %>
      
        <li class="post"> <%= link_to post.body, post %>  |  
        
        <% if post.user.expert %>
          <span class="expert"><%= post.user.fname %></span>
        <% else %>
          <span ><%= post.user.fname %></span>
        <%end%>

        from <%= post.user.restname %> in <%= post.user.restcity %>, <%= post.user.reststate %> </li> 
        
        <% post.comments.each do |comment| %>
        
          <% if comment.post.id == post.id %>       
            <li class="comment"> <%= comment.body %> | by <%= comment.user.fname %> from <%= comment.user.restname %> in <%= comment.user.restcity %>, <%= comment.user.reststate %> <br/> </li> 
          <% else %>

          <% end %>
        <% end %>   
    <% end %>
  </ul>

<br>
<p id="foot">If you would like to become an <span class="expert">expert</span> on this site, click here to email <a href="mailto:admin@tweatonfood.com?subject=Request to become an expert.&body=Say a few words here or send some documentation stating your qualifications on becoming an expert for Tweatonfood. Thanks for your time.">admin@tweatonfood.com</a> with your qualifications for approval. Thanks.</p><br/>


<!-- <div class="imgContainer">
  <img src="assets/dee.jpg" alt="Dee's Diner in Salt Lake City, Ut">
</div>
<div class="imgContainer">
  <img src="assets/bg.jpg" alt="Bamboo Gardens in Miami, Fl">
</div>
<div class="imgContainer">
  <img src="assets/kenkee.jpg" alt="Kenkee in Chicago, Il">
</div> -->